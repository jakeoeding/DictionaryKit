name: CI

on:
  pull_request:

jobs:
  smoke-test:
    runs-on: macos-latest
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: Build
      run: swift build -v

    - name: Run Smoke Test
      run: swift run -v DictionaryKitExample
